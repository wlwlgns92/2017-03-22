	
	1. 메모리
	 ↓
	 JVM (자바 가상 머신) : OS와 JAVA 사이의 중개자 역할
	 	메모리 관리 , Garbage collection (null)관리 
	 	스택기반
	 
	 
	 Garbage collection : 메모리 자동관리 
	 
	 Class Loader : 클래스 불러오기
	 
	 Exectuion Engine : (바이트)코드 명령어 단위로 읽기 (이 다음은 스레드가 자원을 할당 받아 처리) 
								↓	
	 JVM 메모리 영역 (Runtime data Area)	
	 메소드 영역: 클래스(설계도) 멤버(필드, 생성자, 메소드)
	 힙 영역 	 : 객체, 인스턴스, 실제 데이터(정보)  ex) 에서 3이 힙 영역에 포함된다.
	 스택 영역 : 변수명, 주소값(힙주소) 아래의 선언에서 int 정수 까지 스택 영역에 포함된다.
	 
	 ex) int 정수 = 3;
	 
	2. 변수
	 - 기본자료형을 이용한 메모리 할당
	 - new를 사용하지 않음
	 - 데이터 한개를 저장할 수 있는 공간.
	 
	 2.1 배열
	 - 동일한 자료형의 메모리 관리
	 - 순서대로 저장 /인덱스( 0부터 시작)
	 
	 2.2 객체
	 - 클래스 기반으로 메모리 할당 [ new O ]
	 - 여러개의 필드, 메소드 가지고 있음[.연산자]
	 
	 2.3 컬렉션 프레임워크
	 - 메모리 관리해주는 클래스들의 집합
	 - ekdidgks apthem wprhd
	 	1. List
	 		1.1 Arraylist
	 		1.2 Vector
	 		1.3 LinkedList [Queue]
	 		1.4 Stack
	 		
	 	2. SET
	 		2.1 HashSet
	 		2.2 TreeSet
	 	
	 	3. MAP
	 		3.1 Hashtable
	 		3.2 HashMap
	 		3.3 TreeMap
	 